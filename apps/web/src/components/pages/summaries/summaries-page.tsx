import { AppShell } from '../app-shell';

export function SummariesPage() {
  return (
    <AppShell title="Summaries">
      <p className="text-muted-foreground text-sm">Summaries content placeholder.</p>
    </AppShell>
  );
}
