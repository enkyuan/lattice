import { AppShell } from '../../app-shell';

export function SecuritySettingsPage() {
  return (
    <AppShell title="Security Settings">
      <p className="text-muted-foreground text-sm">Security settings content placeholder.</p>
    </AppShell>
  );
}
