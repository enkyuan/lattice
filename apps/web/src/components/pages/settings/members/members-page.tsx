import { AppShell } from '../../app-shell';

export function MembersSettingsPage() {
  return (
    <AppShell title="Members Settings">
      <p className="text-muted-foreground text-sm">Members settings content placeholder.</p>
    </AppShell>
  );
}
