import { AppShell } from '../../app-shell';

export function ProfileSettingsPage() {
  return (
    <AppShell title="Profile Settings">
      <p className="text-muted-foreground text-sm">Profile settings content placeholder.</p>
    </AppShell>
  );
}
