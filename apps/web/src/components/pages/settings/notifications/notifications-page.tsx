import { AppShell } from '../../app-shell';

export function NotificationsSettingsPage() {
  return (
    <AppShell title="Notifications Settings">
      <p className="text-muted-foreground text-sm">Notifications settings content placeholder.</p>
    </AppShell>
  );
}
