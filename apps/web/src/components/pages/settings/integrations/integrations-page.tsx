import { AppShell } from '../../app-shell';

export function IntegrationsSettingsPage() {
  return (
    <AppShell title="Integrations Settings">
      <p className="text-muted-foreground text-sm">Integrations settings content placeholder.</p>
    </AppShell>
  );
}
