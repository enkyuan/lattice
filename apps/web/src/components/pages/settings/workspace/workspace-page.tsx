import { AppShell } from '../../app-shell';

export function WorkspaceSettingsPage() {
  return (
    <AppShell title="Workspace Settings">
      <p className="text-muted-foreground text-sm">Workspace settings content placeholder.</p>
    </AppShell>
  );
}
