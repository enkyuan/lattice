export const SERVICE_NAME = "@lattice/api";

export const AUTH_SESSION_TOKEN_KEYS = [
  "better-auth.session_token",
  "better-auth.session_token.0",
  "session_token",
  "authjs.session-token",
] as const;
